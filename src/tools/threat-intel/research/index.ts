/**
 * Specialized research threat intelligence tools — Phase 4.
 *
 * Deprecated: Individual research vendor RSS tools have been removed.
 * Research intelligence is now gathered via Playwright + DuckDuckGo (Tier 2).
 */

import type { ToolDefinition } from '../../registry.js';

export const researchTools: ToolDefinition[] = [];
export const researchToolCount = 0;
